<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="/static/css/global.css">
    <link rel="stylesheet" type="text/css" href="/static/css/card.css">
    <link rel="stylesheet" type="text/css" href="/static/css/selectChild.css">
    <title>Dokument Auswählen</title>
</head>
<body>
    <div class="card neumorphism">
        <h1 class="header">Dokument Auswählen</h1>
        <div class="divider"></div>
        <% for(var i = 0; i < doc.length; i++) { %>
                <div class="row">
                    <div class="block">
                        <button class="neumorphism" onclick="window.location.href='/historyRender/<%= doc[i].id %>'"> <%= doc[i].document %></button>
                        <a href="sub-title"> <%= doc[i].timestamp %> </a>
                    </div>
                </div>
                <% if(i< doc.length -1){%>
                    <div class="divider-minimal"></div>
                <% } %>
        <% } %>
        <a class="sub-title" href="/selectChild">Zurück</a>
    </div>
    <%- include('./partials/footer'); %>
</body>
</html>